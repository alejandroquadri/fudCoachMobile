import { UserProfile } from '@types';

const initialState: UserProfile = {
  name: '',
  email: '',
  gender: '',
  weight: 0,
  height: 0,
  weightGoal: 0,
  birthdate: '',
};

const profileReducer = (state: UserProfile): UserProfile => {};
